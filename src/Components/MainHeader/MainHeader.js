import React from 'react';

// MainHeader
const MainHeader = ( {headerText} ) => {
    return (
        <header>
            <h1>{headerText}</h1>
        </header>
    );
} // end MainHeader

export default MainHeader;